# MONGODB_URI: The connection string for your LOCAL MongoDB database
# 'localhost:27017' is the default address and port for MongoDB Community Server
# 'cloudcoredb' is the name of the database your application will use (MongoDB will create it if it doesn't exist)
MONGODB_URI=mongodb://localhost:27017/cloudcoredb

# EMAIL_USER: The email address your server will use to send verification emails
EMAIL_USER=your_email@gmail.com # <--- REPLACE WITH YOUR ACTUAL GMAIL ADDRESS

# EMAIL_PASS: The password for your EMAIL_USER.
# IMPORTANT: If using GMAIL, this MUST be an "App Password", NOT your regular Gmail password.
# See the detailed guide below this section on how to get an App Password.
EMAIL_PASS=your_gmail_app_password_here # <--- REPLACE WITH YOUR GMAIL APP PASSWORD

# FRONTEND_URL: The URL where your frontend (index.html) is served by Live Server.
# This is needed for CORS (Cross-Origin Resource Sharing) and for the email verification link.
# Typically, Live Server uses http://127.0.0.1:5500. Double-check your browser's address bar when Live Server is running.
FRONTEND_URL=http://127.0.0.1:5500 # <--- VERIFY AND REPLACE IF DIFFERENT

# SERVER_PORT: The port number on which your Node.js backend server will run.
SERVER_PORT=5000